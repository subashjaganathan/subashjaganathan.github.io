source "https://rubygems.org"

# GitHub Pages builds this site with Jekyll (Ruby). This gem mirrors the
# exact versions GitHub Pages uses, so local `bundle exec jekyll serve`
# matches production.
gem "github-pages", group: :jekyll_plugins

# Local dev convenience
gem "webrick", "~> 1.8"
